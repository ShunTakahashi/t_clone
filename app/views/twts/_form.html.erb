<%= form_with(model: @twt,url: new_twts,local: true) do |form| %>
  <% if @twt.errors.any? %>
    <div id="error_explanation">
      <h2><%= @twt.errors.count %>件のエラーがあります。</h2>
      <ul>

      <% @twt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="blog_title">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="blog_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>

<%= link_to "TOP画面にもどる", blogs_path %>
